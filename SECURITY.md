# Security policy

Fubun Phase 1はローカル専用です。外部ネットワーク通信、localhost HTTP/TCP、任意シェル実行を実装しません。

脆弱性報告には、再現手順、影響、対象commitを含め、公開IssueではなくGitHubのprivate vulnerability reportingを使用してください。秘密情報、個人データ、実データベースを添付しないでください。

Security境界を壊す回帰はP1として扱います。対応対象は最新のdefault branchと直近releaseです。
